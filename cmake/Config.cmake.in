include("${CMAKE_CURRENT_LIST_DIR}/@WT_TARGETS_EXPORT_NAME@.cmake")

include(CMakeFindDependencyMacro) 

IF(MSVC)
  #need to turn off auto linking for boost
  set_property(TARGET WT::wt PROPERTY INTERFACE_COMPILE_DEFINITIONS "BOOST_ALL_NO_LIB=1")
ENDIF(MSVC)

if("@MYSQL_FOUND@")
  find_dependency(MySQL-client CONFIG)
endif()

if("@PNG_FOUND@")
  find_dependency(PNG CONFIG)
endif()

if("@SSL_FOUND@")
  find_dependency(OpenSSL)
endif()

if("@ZLIB_FOUND@")
  find_dependency(ZLIB CONFIG)
endif()